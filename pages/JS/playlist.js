function openPlaylist(name) {
  console.log("Opening playlist:", name);

  // Example actions:
  alert(`Opening ${name}`);

  // You can replace this with:
  // window.location.href = `/playlist.html?name=${name}`;
  // fetch(...)
  // render another component
}

